CREATE TABLE api_test (
                                                          id INTEGER PRIMARY KEY AUTOINCREMENT,
                                                          text_message TEXT NOT NULL
);
